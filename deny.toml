# https://embarkstudios.github.io/cargo-deny/

[licenses]
# Allow only specific licenses
allow = [
    "Apache-2.0",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "ISC",
    "MIT",
    "OpenSSL",
    "Unicode-3.0",
]

# Set confidence threshold for license detection
confidence-threshold = 0.8

[bans]
# Warn about multiple versions of the same dependency
multiple-versions = "allow"

# Warn about wildcard dependencies
wildcards = "deny"

# Deny specific crates
deny = [
    # Non-FIPS crypto modules.
    "ring",
    #
    # This is pending upstream dependency changes:
    # "aws-lc-sys",
]

# Skip certain crates from duplicate checks
skip = []

# Skip certain crate trees from duplicate checks
skip-tree = []

[sources]
# Deny crates from unknown registries
unknown-registry = "deny"

# Deny crates from git sources
unknown-git = "deny"

# Allow crates from crates.io
allow-registry = ["https://github.com/rust-lang/crates.io-index"]

# Allow git sources (if needed, can be empty list to deny all)
allow-git = []
